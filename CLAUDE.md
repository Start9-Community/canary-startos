## How the upstream version is pulled
- dockerTags in `startos/manifest/index.ts`:
  - `schjonhaug/canary-frontend:v<version>`
  - `schjonhaug/canary-backend:v<version>`
- Both must be updated together.

> Fork of schjonhaug/canary-startos.
