import { VersionInfo } from '@start9labs/start-sdk'

export const v_1_4_0_0_b0 = VersionInfo.of({
  version: '1.4.0:0-beta.0',
  releaseNotes: `Monitor individual Bitcoin addresses alongside HD wallets, with automatic transaction lookup via your local Mempool instance when installed on Umbrel. Added the ability to test your ntfy notification setup directly from the app.`,
  migrations: {},
})
